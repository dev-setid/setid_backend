'use strict';

/**
 * conversation-message router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::conversation-message.conversation-message');
