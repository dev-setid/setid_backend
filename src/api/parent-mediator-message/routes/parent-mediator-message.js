'use strict';

/**
 * parent-mediator-message router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::parent-mediator-message.parent-mediator-message');
